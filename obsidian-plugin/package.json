{
  "name": "vaultnexus-obsidian-plugin",
  "version": "0.0.1",
  "private": true,
  "description": "Obsidian plugin shell — talks to the VaultNexus daemon via loopback HTTP.",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "obsidian": "^1.5.0",
    "typescript": "^5.6.0"
  }
}
